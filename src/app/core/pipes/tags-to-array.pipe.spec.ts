import { TagsToArrayPipe } from './tags-to-array.pipe';

describe('TagsToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TagsToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
